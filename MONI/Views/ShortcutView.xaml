<UserControl x:Class="MONI.Views.ShortcutView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ViewModels="clr-namespace:MONI.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="300"
             d:DesignWidth="300"
             d:DataContext="{d:DesignInstance ViewModels:ShortcutViewModel}">

  <Grid Width="400"
        Margin="10">

    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"></RowDefinition>
      <RowDefinition Height="Auto"></RowDefinition>
      <RowDefinition Height="Auto"></RowDefinition>
      <RowDefinition Height="Auto"></RowDefinition>
    </Grid.RowDefinitions>
    <Grid.ColumnDefinitions>
      <ColumnDefinition></ColumnDefinition>
      <ColumnDefinition></ColumnDefinition>
    </Grid.ColumnDefinitions>

    <Label Grid.Column="0"
           Grid.Row="0">Shortcut</Label>
    <TextBox Grid.Column="1"
             Grid.Row="0"
             Margin="2"
             Focusable="True"
             x:Name="shortcutTextBox"
             Text="{Binding Model.Key, UpdateSourceTrigger=PropertyChanged}"></TextBox>

    <Label Grid.Column="0"
           Grid.Row="1">Expansion</Label>
    <TextBox Grid.Column="1"
             Grid.Row="1"
             Margin="2"
             Text="{Binding Model.Expansion, UpdateSourceTrigger=PropertyChanged}"></TextBox>

    <Label Grid.Column="0"
           Grid.Row="2">Ganzen Tag ersetzen</Label>
    <CheckBox Grid.Column="1"
              Grid.Row="2"
              Margin="2"
              IsChecked="{Binding Model.WholeDayExpansion, UpdateSourceTrigger=PropertyChanged}"></CheckBox>

    <Button Grid.Column="0"
            Grid.Row="3"
            Command="{Binding CancelCommand, Mode=OneWay}">Abbrechen</Button>
    <Button Grid.Column="1"
            Grid.Row="3"
            Command="{Binding SaveCommand, Mode=OneWay}">Speichern</Button>
  </Grid>

</UserControl>
